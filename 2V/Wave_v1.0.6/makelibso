# -*- MakeFile -*-

#target: dependencies
#	action

CFLAGS = -g -Wall

all: libwave.so

libwave.so: wave.o list.o
	gcc -shared wave.o list.o -o libwave.so

wave.o: wave.c list.h
	gcc -c $(CFLAGS) wave.c -fpic

list.o: list.c list.h
	gcc -c $(CFLAGS) list.c -fpic
